<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>seaco</groupId>
		<artifactId>tp-parent</artifactId>
		<version>0.0.1-SNAPSHOT</version>
	</parent>
	
	<artifactId>tp-world</artifactId>
	<packaging>war</packaging>

	<name>tp-world</name>
	<url>http://maven.apache.org</url>

	<properties>
		<api-common.version>0.0.1-SNAPSHOT</api-common.version>
		<tp-proxy.version>0.0.1-SNAPSHOT</tp-proxy.version>
		<servlet.jstl.version>1.1.2</servlet.jstl.version>
		<tp.qiniu.version>0.0.1-SNAPSHOT</tp.qiniu.version>
		<org.apache.poi.version>3.10-FINAL</org.apache.poi.version>
  		<poi.version>3.9</poi.version>
	</properties>

	<dependencies>
	   <dependency>
	   		<groupId>seaco</groupId>
	   		<artifactId>tp-proxy</artifactId>
			<version>${tp-proxy.version}</version>
	   </dependency>
	   <dependency>
			<groupId>seaco</groupId>
			<artifactId>tp-api-common</artifactId>
			<version>${api-common.version}</version>
		</dependency>
		<dependency>
			<groupId>tomcat</groupId>
			<artifactId>servlet-api</artifactId>
			<version>5.5.23</version>
		</dependency>
		<dependency>
            <groupId>javax.servlet</groupId>
            <artifactId>javax.servlet-api</artifactId>
            <version>3.0.1</version>
            <scope>test</scope>
        </dependency>
		<dependency>
			<groupId>seaco</groupId>
			<artifactId>tp-qiniu</artifactId>
			<version>${tp.qiniu.version}</version>
		</dependency>
		<dependency>
			<groupId>org.springframework</groupId>
			<artifactId>spring-context-support</artifactId>
			<version>${spring.version}</version>
		</dependency>
		<dependency>
			<groupId>org.apache.poi</groupId>
			<artifactId>poi</artifactId>
			<version>${org.apache.poi.version}</version>
		</dependency>
		<dependency>
			<groupId>org.apache.poi</groupId>
			<artifactId>poi-ooxml</artifactId>
			<version>${poi.version}</version>
		</dependency>
	</dependencies>
	

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<configuration>
					<source>1.7</source>
					<target>1.7</target>
					<encoding>UTF-8</encoding>
				</configuration>
			</plugin>
			
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-war-plugin</artifactId>
				<version>2.3</version>
				<configuration>
					<warName>ROOT</warName>
				</configuration>
			</plugin>
			
			<plugin>
				<artifactId>maven-resources-plugin</artifactId>
				<version>2.5</version>
				<executions>
					<execution>
						<id>copy-resources</id>
						<phase>validate</phase>
						<goals>
							<goal>copy-resources</goal>
						</goals>
						<configuration>
							<outputDirectory>target/classes/config</outputDirectory>
							<resources>
								<resource>
									<directory>src/main/resources/config/${cfgpath}</directory>
									<filtering>true</filtering>
								</resource>
							</resources>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
	<profiles>
		<profile>
			<id>test</id>
			<properties>
				<env>test</env>
				<cfgpath>test</cfgpath>
			</properties>
		</profile>
		
		<profile>
			<id>dev</id>
			<properties>
				<env>dev</env>
				<cfgpath>dev</cfgpath>
			</properties>
		</profile>
	</profiles>
</project>